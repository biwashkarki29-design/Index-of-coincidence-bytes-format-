Byte-Based Vigenère Key Length Detection using Index of Coincidence
This Python script calculates the Index of Coincidence (IC) for a ciphertext file and identifies likely key lengths for a byte-based Vigenère (repeating-key XOR) cipher.
Works with binary files (bytes 0–255), not just alphabetic text.
Helps to estimate the key length before attempting decryption.
